# Copy to .env and fill in. Never commit .env.
LITELLM_MASTER_KEY=sk-poc-changeme
ANTHROPIC_API_KEY=
# Address the model host by VCN-internal DNS, never by IP. The private IP
# changes on every rebuild; this name does not.
VLLM_API_BASE=http://phi-model.private.phipoc.oraclevcn.com:8000/v1
OCI_REGION=us-ashburn-1
